{
  "buildCommand": "npx prisma generate && (npx prisma db push --accept-data-loss || echo 'DB push skipped') && pnpm run build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "remix"
}
